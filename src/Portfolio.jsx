import { BrowserRouter } from "react-router-dom"
import { AppRouter } from "./router"

export const Portfolio = () => {
    return (
        <BrowserRouter>
            <AppRouter />
        </BrowserRouter>
    )
}
